/**
 * Application configuration.
 *
 * Gemini runs on the backend with Google Application Default Credentials —
 * do not put a Gemini API key in the frontend.
 */

export const GEMINI_READY = true;

export const IS_DEMO = false;
