{
  "_comment": "DukanSaathi frontend config. Supabase URL + anon key live in NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY (REACT_APP_* still works during cutover). Gemini runs on the backend with ADC — do not put a Gemini API key here."
}
